create database canekerp;
use canekerp

create table users (
    id int,
    FirstName varchar(255),
    LastName varchar(255)
);

inseret into users (id, FirstName, LastName) values (1, 'Victor', 'Jimenez')